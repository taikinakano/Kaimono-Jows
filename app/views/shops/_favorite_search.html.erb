  <%= form_with model: shop, url: favorite_search_shops_path, method: :get, local: true do |form| %>
  <%= form.select :keyword, [ ['投稿が新しい順', 'new'],
                            ['投稿が古い順', 'old'],
                            ['いいねが多い順', 'likes'],
                            ['いいねが少ない順', 'dislikes'],
                          ] %>
  <%= form.submit "並び替える" %>
  <% end %>